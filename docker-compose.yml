version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sleep-api
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8787:8787"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=8787
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN:-30d}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEEPSEEK_API_URL=${DEEPSEEK_API_URL:-https://api.deepseek.com/v1/chat/completions}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL:-deepseek-chat}
      - SILICONFLOW_API_KEY=${SILICONFLOW_API_KEY}
      - SENTRY_DSN=${SENTRY_DSN}
      - QUOTA_GUEST_CHAT=${QUOTA_GUEST_CHAT:-0}
      - QUOTA_GUEST_INTERPRET=${QUOTA_GUEST_INTERPRET:-0}
      - QUOTA_USER_CHAT=${QUOTA_USER_CHAT:-0}
      - QUOTA_USER_INTERPRET=${QUOTA_USER_INTERPRET:-0}
      - TENCENT_SMS_SECRET_ID=${TENCENT_SMS_SECRET_ID}
      - TENCENT_SMS_SECRET_KEY=${TENCENT_SMS_SECRET_KEY}
      - TENCENT_SMS_SDK_APP_ID=${TENCENT_SMS_SDK_APP_ID}
      - TENCENT_SMS_SIGN_NAME=${TENCENT_SMS_SIGN_NAME}
      - TENCENT_SMS_TEMPLATE_ID=${TENCENT_SMS_TEMPLATE_ID}
      - TENCENT_SMS_REGION=${TENCENT_SMS_REGION:-ap-guangzhou}
      - SMS_MOCK=${SMS_MOCK:-}
      - SMS_MOCK_CODE=${SMS_MOCK_CODE:-123456}
      - SMS_CODE_TTL_SEC=${SMS_CODE_TTL_SEC:-300}
      - SMS_SEND_INTERVAL_SEC=${SMS_SEND_INTERVAL_SEC:-60}
      - WECHAT_APP_ID=${WECHAT_APP_ID}
      - WECHAT_APP_SECRET=${WECHAT_APP_SECRET}
      - MQTT_CONTROL_URL=${MQTT_CONTROL_URL:-http://host.docker.internal:8790}
      - MQTT_ADMIN_TOKEN=${MQTT_ADMIN_TOKEN}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8787/health', (r) => r.statusCode === 200 ? process.exit(0) : process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
